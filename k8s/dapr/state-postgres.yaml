apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: datastore
  namespace: dapr-app
spec:
  type: state.postgresql
  # Note: setting "version" to "v2" is required to use the v2 of the component
  version: v2
  metadata:
  - name: connectionString
    value: "host=postgres-0.postgres.dapr-app.svc user=postgres password=thisispassword port=5432 dbname=postgres sslmode=disable"